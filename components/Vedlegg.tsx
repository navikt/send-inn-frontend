import React, { FC, ReactElement } from 'react';

type VedleggProps = {};

const Vedlegg: FC<VedleggProps> = (
    {
        /* destructured props */
    },
): ReactElement => {
    return <div>Hello world</div>;
};
